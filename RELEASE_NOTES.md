# DiskAnalyzer 0.1.2

首个可公开试用的 Windows x64 MVP 版本。

## 用户可以做什么

- 查看电脑中的固定磁盘及容量；
- 后台扫描整个磁盘中的真实文件；
- 查看文件名、完整路径、大小、类型和修改时间；
- 扫描完成后直接看到最大的文件；
- 点击列标题切换升序和降序；
- 在资源管理器中打开文件所在位置；
- 随时取消扫描。

## 下载方式

在 GitHub 页面右侧打开 **Releases**，下载：

```text
DiskAnalyzer-0.1.2-win-x64.zip
```

解压后文件夹根目录只有一个主程序：

```text
DiskAnalyzer.exe
```

双击即可运行，不需要安装 .NET。

## 注意

- 当前程序尚未代码签名，Windows SmartScreen 可能显示“未知发布者”；
- 无权访问的目录会被跳过；
- 默认不进入目录联接点和目录符号链接；
- MVP 暂不包含删除、搜索、重复文件或缓存清理功能。
