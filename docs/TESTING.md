# DiskAnalyzer 测试与交付记录（Step 6）

## 自动化测试

Release 配置测试结果：

- 总计：22；
- 通过：22；
- 失败：0；
- 跳过：0；
- 编译警告：0；
- 编译错误：0。

覆盖范围：

- 普通目录和嵌套目录扫描；
- 多工作线程扫描不丢失、不重复文件；
- 扫描前取消和扫描中取消；
- 缺失根目录；
- 目录重解析点不向下遍历；
- 中文、空格和 `.iso` 文件路径；
- 文件数量、累计大小和进度结果；
- 文件大小单位边界；
- 文件名与扩展名派生；
- 扫描选项参数校验；
- 固定磁盘容量关系；
- 资源管理器参数保留中文和空格；
- ViewModel 初始状态。

## 真实目录扫描

对包含源码、构建产物和 NuGet 缓存的开发工作区进行 Release 扫描：

- 文件数：3,668；
- 文件总大小：392,039,480 Bytes；
- 扫描耗时：212 ms；
- 托管总分配：4.0 MB；
- 保留结果内存：1.2 MB。

该目录包含源码、构建输出、NuGet 包、多层目录和不同文件类型。

## 发布包验证

- 目标：Windows x64；
- 形式：.NET 8 自包含单文件；
- 版本：0.1.2；
- EXE 大小：71,919,213 Bytes；
- ZIP 大小：66,301,757 Bytes；
- 从 ZIP 解压后的 EXE 启动验证：通过；
- 产品名：DiskAnalyzer；
- 文件版本：0.1.2.0；
- 多尺寸 Windows 图标已从最终 EXE 提取并人工检查；
- NuGet 在线还原和漏洞审计：无警告。

SHA-256：

```text
5297cb97bb414fa82b24e1a7c65667764c2d07ed02910e268e70c44dc8217dcb
```

## 尚未在当前开发机验证

- 真实 4 TB HDD 的冷缓存全盘扫描；
- 100 万真实文件同时保留在 WPF DataGrid 时的峰值内存；
- Windows 10 实机；
- 非 x64 Windows；
- 代码签名和 SmartScreen 信誉；
- 管理员与普通用户账户之间的权限差异。

这些项目不影响当前 MVP 的构建和启动，但应在公开发布或扩大用户范围前纳入兼容性测试。
